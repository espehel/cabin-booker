
const initialState = {id: 'u1', name: 'Espen Hellerud', email: 'espenhellerud5@gmail.com'};

export default (state = initialState, action) => {
  switch (action.type) {
    default: return state;
  }
}